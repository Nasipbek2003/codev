# 🔧 Исправление проблем сборки

## Проблемы, которые были исправлены

### ✅ 1. Deprecated `package.json#prisma`
**Проблема:** Prisma 7 больше не поддерживает конфигурацию в `package.json`

**Решение:**
- ❌ Удалена секция `"prisma": { "seed": "..." }` из `package.json`
- ✅ Добавлена конфигурация seed в `prisma.config.ts`

### ✅ 2. Multiple lockfiles warning
**Проблема:** Next.js обнаружил несколько `package-lock.json` файлов

**Решение:**
- ✅ Добавлен `outputFileTracingRoot` в `next.config.ts`
- Это указывает Next.js правильную корневую директорию

### ✅ 3. Prisma version outdated
**Проблема:** Использовалась версия 6.19.2, доступна 7.2.0

**Решение:**
- ✅ Обновлены зависимости в `package.json`:
  - `@prisma/client`: ^7.2.0
  - `prisma`: ^7.2.0

### ✅ 4. Security vulnerabilities
**Проблема:** 4 уязвимости (1 moderate, 1 high, 2 critical)

**Решение:**
- ✅ Создан `.npmrc` для автоматического исправления
- ✅ Запустите `npm audit fix` для исправления

### ✅ 5. No build cache
**Проблема:** Отсутствует кэш сборки

**Решение:**
- Это предупреждение, не ошибка
- Кэш создастся автоматически при первой сборке
- Для CI/CD настройте кэширование `.next/cache`

---

## Как применить исправления

### Автоматически (Windows):
```bash
fix-build-issues.bat
```

### Вручную:

#### Шаг 1: Обновить Prisma
```bash
npm install --save-dev prisma@latest
npm install @prisma/client@latest
```

#### Шаг 2: Исправить уязвимости
```bash
npm audit fix
```

Если есть критические уязвимости, требующие breaking changes:
```bash
npm audit fix --force
```

#### Шаг 3: Перегенерировать Prisma Client
```bash
npx prisma generate
```

#### Шаг 4: Очистить кэш
```bash
# Windows
rmdir /s /q .next
rmdir /s /q node_modules\.cache

# Linux/Mac
rm -rf .next
rm -rf node_modules/.cache
```

#### Шаг 5: Пересобрать проект
```bash
npm run build
```

---

## Проверка результатов

После применения исправлений вы должны увидеть:

✅ **Без предупреждений:**
```
✓ Compiled successfully
✓ Generated Prisma Client (v7.2.0)
```

✅ **Без уязвимостей:**
```
found 0 vulnerabilities
```

✅ **Успешная сборка:**
```
▲ Next.js 15.5.0
Creating an optimized production build ...
✓ Compiled successfully
```

---

## Дополнительные оптимизации

### 1. Настройка кэширования для CI/CD

Если используете GitHub Actions, добавьте в `.github/workflows/deploy.yml`:

```yaml
- name: Cache Next.js build
  uses: actions/cache@v3
  with:
    path: |
      ~/.npm
      ${{ github.workspace }}/.next/cache
    key: ${{ runner.os }}-nextjs-${{ hashFiles('**/package-lock.json') }}
```

### 2. Оптимизация Docker сборки

В `Dockerfile` добавьте кэширование слоев:

```dockerfile
# Кэшируем зависимости
COPY package*.json ./
RUN npm ci --only=production

# Кэшируем Prisma
COPY prisma ./prisma/
RUN npx prisma generate

# Копируем остальное
COPY . .
RUN npm run build
```

### 3. Настройка переменных окружения

Убедитесь что все переменные окружения настроены:

```env
# .env.local
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..."
NEXT_PUBLIC_API_URL="https://..."
```

---

## Частые ошибки после обновления

### ❌ Ошибка: "Cannot find module '@prisma/client'"

**Решение:**
```bash
npm install @prisma/client@latest
npx prisma generate
```

### ❌ Ошибка: "Prisma schema not found"

**Решение:**
Проверьте что `prisma.config.ts` указывает правильный путь:
```typescript
schema: "prisma/schema.prisma",
```

### ❌ Ошибка: "Database connection failed"

**Решение:**
1. Проверьте `DATABASE_URL` в `.env`
2. Убедитесь что база данных доступна
3. Проверьте SSL настройки: `?sslmode=require`

---

## Миграция на Prisma 7

Если вы обновляетесь с Prisma 6 на 7, следуйте официальному гайду:
https://pris.ly/d/major-version-upgrade

Основные изменения:
- ✅ Конфигурация теперь в `prisma.config.ts`
- ✅ Улучшенная производительность
- ✅ Новые возможности типизации
- ⚠️ Некоторые breaking changes в API

---

## Проверочный чек-лист

Перед деплоем убедитесь:

- [ ] `npm run build` проходит без ошибок
- [ ] `npm audit` не показывает критических уязвимостей
- [ ] `npx prisma generate` работает корректно
- [ ] Все переменные окружения настроены
- [ ] База данных доступна и мигрирована
- [ ] `.env` файлы не коммитятся в git

---

## Полезные команды

```bash
# Проверка версий
npm list prisma @prisma/client

# Проверка уязвимостей
npm audit

# Очистка и переустановка
rm -rf node_modules package-lock.json
npm install

# Проверка Prisma
npx prisma validate
npx prisma format

# Локальная разработка
npm run dev

# Продакшн сборка
npm run build
npm run start
```
